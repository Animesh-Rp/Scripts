# Userscripts

Tampermonkey / Greasemonkey / Violentmonkey scripts. One `.user.js` (or `.js`) per file.

**Convention:** filename matches the script's `@name` header, with spaces as underscores. Keep the userscript metadata block at the top — it's what the script manager reads.
